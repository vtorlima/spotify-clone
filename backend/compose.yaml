services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=spotify_db'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=postgresql'
    ports:
      - '5432:5432'
    expose:
      - '5432'
    volumes:
      - './spotify_data:/var/lib/postgresql'